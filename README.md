# K3M2323 Django

Initial repository setup for the Django project.

## Security

- Keep secrets in a local `.env` file only.
- Do not commit virtual environments, SQLite databases, logs, media uploads, or private keys.
- Use `.env.example` as the safe template for required environment variables.
- Keep dependencies updated with Dependabot alerts and pull requests.
